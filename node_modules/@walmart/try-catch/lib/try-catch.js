"use strict";

module.exports = function (fn1, errFn) {
  try {
    return fn1();
  } catch (err) {
    if (typeof errFn === "function") {
      return errFn(err);
    } else {
      return errFn;
    }
  }
};
