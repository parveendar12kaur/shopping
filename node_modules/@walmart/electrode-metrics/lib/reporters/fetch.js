"use strict";

const base = require("./base");

module.exports = (res, duration, options) => {
  options = options || {};
  base.sendEvent("fetch", {
    status: res && res.status,
    duration,
    serviceName: options.serviceName
  });
};
