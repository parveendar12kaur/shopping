"use strict";

const MIN_REPORTING_INTERVAL_MS = 99;
// interval to measure eventloop latency into the metrics for reporting
const REPORTING_INTERVAL_MS = 1000;
// if event loop latency exceeds this then update high water
const BLOCKED_THRESHOLD_MS = 0;

module.exports = {
  MIN_REPORTING_INTERVAL_MS,
  REPORTING_INTERVAL_MS,
  BLOCKED_THRESHOLD_MS
};
