"use strict";

const context = Symbol.for("electrode-log-transaction.symbols.context");
const request = Symbol.for("electrode-logging.symbols.request");
const response = Symbol.for("electrode-logging.symbols.response");
const entryOpt = Symbol.for("electrode-logging.symbols.entryOpt");

module.exports = {
  context,
  request,
  response,
  entryOpt
};
