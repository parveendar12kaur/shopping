"use strict";

//
// yyyy-MM-dd HH:mm:ss.SSS
//

module.exports = function makeCurrentTime(x) {
  const d = x || new Date();
  //server almost wants a valid regular ISO string, by replacing the "T" and removing "Z" we align
  return d.toISOString().replace("T", " ").replace("Z", "");
};
