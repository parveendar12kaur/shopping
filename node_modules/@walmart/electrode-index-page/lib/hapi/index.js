"use strict";
const hapi16 = require("./index-hapi16");
const hapi17 = require("./index-hapi17");

const { universalHapiPlugin } = require("electrode-hapi-compat");

const pkgContent = require("../../package.json");

const pkg = {
  name: pkgContent.name,
  version: pkgContent.version
};

const registers = {
  hapi17,
  hapi16
};

module.exports = universalHapiPlugin(registers, pkg);
