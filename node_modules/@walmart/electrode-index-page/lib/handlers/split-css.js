"use strict";

module.exports = function(context) {
  const chunk = this.props.chunk || "main";
  const chunkAssets = context.user.routeOptions.__internals.assets[chunk];

  if (!chunkAssets || !chunkAssets.splitCss) return undefined;

  let html = "";
  for (const css of chunkAssets.splitCss) {
    html += `<link rel="stylesheet" type="text/css" href="${css}"/>\n`;
  }

  return `<!--[if IE 9]>\n${html}<![endif]-->\n`;
};
