"use strict";

module.exports = obj => {
  if (obj) {
    const keys = Object.keys(obj);
    if (keys.length > 0) {
      return ` ${Object.keys(obj)
        .map(k => `${k}="${obj[k]}"`)
        .join(" ")}`;
    }
  }

  return "";
};
