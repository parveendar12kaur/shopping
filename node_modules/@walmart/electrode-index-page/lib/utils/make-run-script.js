"use strict";

const getClientScript = require("./get-client-script");

module.exports = function(name, param = "", tag = null) {
  const scriptFunc = getClientScript(name);

  tag = tag ? `;/*${tag}*/` : ";";

  return `(${scriptFunc.toString()})(${param})${tag}`;
};
