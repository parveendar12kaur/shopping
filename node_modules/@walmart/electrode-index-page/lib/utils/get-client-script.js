"use strict";

/* eslint-disable global-require */

const Path = require("path");

module.exports = function(file) {
  return require(Path.join(
    "../../resources",
    process.env.NODE_ENV === "production" ? "minified" : "",
    file
  ));
};
