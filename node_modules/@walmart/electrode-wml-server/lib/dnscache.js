"use strict";

const _ = require("lodash");
const dnscache = require("dnscache");

module.exports = function(options) {
  dnscache(
    _.merge(
      {
        enable: true,
        ttl: 5,
        cachesize: 1000
      },
      options
    )
  );
};
