"use strict";

class ServiceToConfigMap {
  constructor() {
    this.map = {};
  }

  set(varId, configName) {
    this.map[`${varId}-${configName}`] = true;
  }

  check(varId, configName) {
    return this.map[`${varId}-${configName}`] === true;
  }
}

module.exports = ServiceToConfigMap;
