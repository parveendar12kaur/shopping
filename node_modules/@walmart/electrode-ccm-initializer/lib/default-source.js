"use strict";

const Promise = require("bluebird");

class DefaultSource {
  constructor(config) {
    this.config = config;
  }

  load() {
    return Promise.resolve(this.config.defaults || {});
  }
}

module.exports = DefaultSource;
