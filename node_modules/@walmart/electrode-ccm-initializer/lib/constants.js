"use strict";

const SECONDS_PER_MIN = 60;
const MINUTES_PER_HOUR = 60;
const MS_PER_SEC = 1000;
const ONE_MINUTE_MS = SECONDS_PER_MIN * MS_PER_SEC;
const ONE_HOUR_MS = MINUTES_PER_HOUR * ONE_MINUTE_MS;
const REFRESH_PATH = "/electrode/services/ccm/refresh";

module.exports = {
  SECONDS_PER_MIN,
  MINUTES_PER_HOUR,
  MS_PER_SEC,
  ONE_MINUTE_MS,
  ONE_HOUR_MS,
  REFRESH_PATH
};
