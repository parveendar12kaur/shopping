// This Stylus file also exists in `@walmart/feds-ny-core'. You will most likely be importing this
// from `@walmart/feds-ny-core/src/error-page/styles/error-page`.

// Any changes to these styles should also be made in the `feds-ny-core` version as well.

// This package was created to prevent a circular dependency in some packages, such as
// `discovery-webapp` requring `feds-ny-core` for `<ErrorPage>`. Since most apps implicitly get
// `feds-error-page` through a webapp package and won't import it directly, the Stylus file was also
// made available in `feds-ny-core`.

// This file depends on variables from the `lithe-extras` base styles (e.g. $xs-spacing, $blue-dark,
// etc) which you will likely already have in your app. It is located at:
// `@walmart/lithe-extras/lib/tenants/new-york/base`.

.error-page__header {
  padding-top: $xs-spacing;
  padding-bottom: $xs-spacing;
  padding-left: $l-spacing;
  background: $blue-dark;
}

.error-page__content {
  margin: 0 auto $l-spacing;
  padding: $m-spacing;
  max-width: 550px;
  width: 100%;
  font-size: 22px;
  text-align: center;
  line-height: 1.4;
}

.error-page__logo {
  margin-top: $m-spacing;
}

.error-page__title {
  font-weight: $font-bold;
  font-size: 64px;
  letter-spacing: .1px;
}

.error-page__message {
  color: $charcoal-grey;
}

.error-page__spark {
  color: $yellow-spark;
  font-size: $font-sizes.l;
}

.error-page__links {
  margin-top: $l-spacing;
}

.error-page__link {
  text-decoration: underline;
}
