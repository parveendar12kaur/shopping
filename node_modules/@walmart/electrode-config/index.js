"use strict";

let config;
const makeConfig = require("./lib/make-config");

const data = {
  initialize: (options) => {
    if (!config) {
      config = makeConfig(options);
    }
    return config;
  }
};

Object.defineProperty(data, "config", {
  get: () => data.initialize()
});

module.exports = data;
