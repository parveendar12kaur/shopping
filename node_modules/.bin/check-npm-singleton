#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/bash" ]; then
  "$basedir/bash"  "$basedir/../@walmart/electrode-npm-singleton/bin/check-npm-singleton.sh" "$@"
  ret=$?
else 
  bash  "$basedir/../@walmart/electrode-npm-singleton/bin/check-npm-singleton.sh" "$@"
  ret=$?
fi
exit $ret
